/**
 * Outbound notifications (email, WhatsApp Business, SMS).
 * Implement adapters here when connecting providers; call from services, not from React components.
 */
export const notifications = {
  // sendTransactionalEmail(...) { ... }
} as const;
