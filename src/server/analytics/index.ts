/**
 * Server-side analytics hooks (product telemetry, conversion events).
 * Wire implementations when you add PostHog, Plausible, or internal event sinks.
 */
export const analytics = {
  // trackEvent(name: string, props?: Record<string, unknown>) { ... }
} as const;
